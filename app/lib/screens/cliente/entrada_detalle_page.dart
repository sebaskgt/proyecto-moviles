import 'package:flutter/material.dart';

class EntradaDetallePage extends StatelessWidget {
  String idEvento;
  int numero_entrada;
  EntradaDetallePage(this.idEvento, this.numero_entrada, {Key? key})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
